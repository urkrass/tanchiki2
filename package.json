{
  "name": "tanchiki",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "game:smoke": "vitest run src/game/game.test.ts",
    "harness:validate": "node ./.agentic-harness/harness-wrapper.mjs validate",
    "harness:smoke": "node ./.agentic-harness/harness-wrapper.mjs smoke",
    "validate": "npm run test && npm run build && npm run harness:validate && npm run harness:smoke"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "vitest": "^4.1.9"
  }
}
