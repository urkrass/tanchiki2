# Online Battle Human WAN Playtest Checklist

Status: **PENDING EXTERNAL PARTICIPANTS**. This gate has not been run or claimed.

Run 10-20 real 2v2 matches with human participants. Include home Wi-Fi, a mobile hotspot, and at least one participant in another city or country. Never record room keys, reconnection tokens, IP addresses, or credentials.

For each match record only:

- anonymous participant labels A-D and broad connection type;
- date, build commit, match ordinal, and whether all four saw the same score/winner/reason;
- whether Ready/countdown behavior was understandable;
- reconnect outcome and approximate user-visible interruption duration;
- connection-quality labels and the aggregate after-action network summary;
- stuck room, duplicate tank, stale input, fog leak, unexplained disconnect, or cleanup failure;
- concise usability notes about Field Briefing, copy/join, touch controls, and result clarity.

Pass requires no divergent result, fog leak, duplicate slot/tank, stale-socket mutation, stuck room, or cleanup failure. Any failure must preserve its seed when synthetic, the exact commit, a redacted trace, and reproduction steps.

Human sign-off:

- Participants/locations represented: _pending_
- Matches completed: `0 / 10 minimum`
- Reviewer: _pending_
- Evidence location: _pending_
- Decision: `PENDING`
